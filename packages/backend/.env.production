# ============= CONFIGURACIÓN BACKEND PARA PRODUCCIÓN =============
# 🚀 Configuración simplificada - sin BD ni autenticación

# Cambiar de development a production
NODE_ENV=production
PORT=3001

# CORS - Permitir acceso desde el dominio
CORS_CREDENTIALS=true
CORS_ORIGIN= # http://santacruz.go.cr/becasmunicipales   cuando este disponible el dominio

# 📧 SMTP - Tu configuración actual funciona perfecto en producción
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587          
SMTP_SECURE=false       
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Becas <no-reply@dominio.com>"

# Email de destino
MAIL_TO_BECAS=

# Template de email
MAIL_USE_TEMPLATE=true

# 📁 Uploads - Cambiar a ruta absoluta para Windows
UPLOADS_DIR=C:/proyectos/beca-municipal-system/uploads